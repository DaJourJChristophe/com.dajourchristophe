services:
  ci:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile.ci
    container_name: com-dajour-ci
    working_dir: /workspace
    environment:
      CI: "true"
      HOST: 127.0.0.1
      PORT: 3100
    command: npm run ci
